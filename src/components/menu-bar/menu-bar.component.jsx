import React from 'react';

const MenuBar = (props) => {

  return (
    <nav class="nav">
      <a class="nav-link active" href="#">Add Category</a>
      <a class="nav-link" href="#">Add Tab</a>
      <a class="nav-link" href="#">Categories</a>
      <a class="nav-link" href="#">Tabs</a>
    </nav>
  )
}

export default MenuBar;